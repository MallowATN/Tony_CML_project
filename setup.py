from setuptools import find_packages, setup
# Go to directory -> $... python setup.py install -> python setup.py build
setup(
    name='src',
    packages = find_packages(),
    version='0.1.0',
    description = 'CML project',
    author='Tony',
    license='',
    
)